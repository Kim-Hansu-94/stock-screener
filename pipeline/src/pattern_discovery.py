"""Gold Standard 바닥 특성 기반 후보 종목 발굴 — 파이프라인 사전 계산 모듈 v2.

v2 개선 사항:
  - 하락률 기준: 5개월 고점 → 52주 최고가 (KIS 1년치 이력 직접 사용, lookback_days=380)
  - 가중치 재조정: 하락률 0.3 / 소진일수 0.4 / 거래량 0.3
  - VCP 보너스: ATR10 / ATR50 ≤ 0.6 이면 +0.10점 (변동성 압축)
  - 이평선 정배열 보너스: 현재가 > SMA5 > SMA10 > SMA20 이면 +0.10점
  - NumPy 벡터 연산으로 내부 지표 계산 최적화
  - MIN_SCORE 미달 종목 자동 드롭 후 상위 TOP_N 반환

v3 (2026-09-14, 『매매의 기술』 근거):
  - 거래량 트리거에 **봉 방향 조건** 추가 — 대량거래 + (양봉 또는 십자형)일 때만
    `volume_triggered`. 예전에는 거래량 2배만 보고 투매(대량거래 장대음봉)에도
    ⚡ 배지가 붙었다 (`_is_volume_trigger_today` 주석 참고)
  - 추천 결과에 집계용 원본 수치(`drawdown_pct`·`days_since_low`·`vol_ratio`·
    `vcp`·`ma_align`)를 함께 실어 `recommendation_history`에 남긴다 — 나중에
    "어떤 특성의 후보가 잘 맞았나"로 성적을 쪼개려면 추천 시점 수치가 필요하다

v4 (2026-09-14, **백테스트 검증 — 결론: 채점 공식은 v3 그대로 둔다**):

  두 책에서 뽑은 개선안 네 가지를 3년 워크포워드 재생으로 판정했다
  (`pipeline/research/backtest_pattern_features.py`, 표본 694건 + 하락률 스윕
  10,433건). **네 가지 모두 채택되지 않았다.** 아래는 "이미 시험해 봤고 왜
  안 됐는지"의 기록이다 — 같은 아이디어를 다시 꺼내기 전에 읽을 것.

  **① 만점 지점 당기기 (하락률 90%→65%, 소진일수 60일→50일) — 기각**
  "더 깊게·더 오래 가도 성과가 안 좋아지는데 왜 계속 가산하나"가 근거였다.
  당겨 보니 **점수가 변별력을 잃었다**: 70점 이상이 87건 → 457건(표본의 65%)이
  되어 구간 구분이 무의미해졌고, 나쁜 구간은 사라진 게 아니라 60~69점으로
  내려갔다(중간값 -1.46%·승률 47.4%). 고치려던 1~5위 성적은 중간값
  0.00% → **-6.22%**, 승률 49.6% → **44.6%**로 더 나빠졌다.

  **② 저점 높이기 보너스 (+0.10) — 기각, 관측만 유지**
  `_is_higher_low` 주석 참고. 관측에서는 가장 뚜렷했으나(중간값 11.72% vs 8.62%)
  가산점을 주자 해당 종목이 272 → 398건으로 늘며 우위가 사라졌다.

  **③ 이평 정배열 보너스 제거 — 기각(되돌림)**
  관측은 분명했다 — 정배열 종목이 오히려 나빴다(60거래일 중간값 5.86%·승률
  56.5% vs 정배열 아님 10.84%·59.3%). 그래서 ①②를 되돌린 뒤 ③만 남겨
  재검증했는데(표본 674건), **전체가 나아지지 않았다**: 순위 1~5위 중간값
  0.00% → -2.25%·승률 49.6% → 47.3%, 60~69점 구간 20.77% → 12.44%,
  2025년 13.82% → 12.52%. 나아진 구간(6~10위 2.19 → 6.78)과 나빠진 구간이
  섞여 순효과가 없다.

  **결정적인 단서는 저점 높이기 쪽에서 나왔다.** ③만 뺐을 뿐인데 원본의
  저점 높이기 우위(11.72%·59.9% vs 8.62%·57.8%)가 **역전됐다**
  (7.56%·55.9% vs 10.60%·60.1%). 보너스 하나를 건드리면 상위 20에 뽑히는
  종목 구성이 통째로 바뀌고, 그러면 다른 조건의 관측값까지 같이 뒤집힌다.
  즉 **"정배열 종목이 나빴다"는 관측은 "정배열 보너스를 빼면 좋아진다"의
  근거가 되지 못한다.** 근거가 없으므로 되돌렸다.

  **④ 하락 속도 / 50% 룰 — 채택 안 함**
  책과 반대로 급락한 종목이 가장 나빴고, 50% 룰은 승률 56.6 vs 56.2로
  차이가 없었다.

  **검증돼서 그대로 둔 것 — `MIN_DRAWDOWN`(55%)**
  하한을 25%까지 낮춰 재생해 보니 45~55% → 55~65%에서 60거래일 중간값이
  5.92% → 14.06%로 뛰고 승률도 58.0% → 64.2%로 올랐다(250거래일도 같은 자리에서
  33.18 → 65.45). 계단이 정확히 55%에 있다. 표본 1439/749건에 쏠림 0.7/0.9%로
  이번 작업에서 가장 깨끗한 증거다. 올릴 근거도 없다 — 65~75%는 중간값이 9.68로
  오히려 떨어지고 85%+는 25종목에 최다종목 21.6%라 못 믿는다.

  **그래서 이번 작업이 남긴 것**은 채점 변경이 아니라 두 가지다:
  (1) 3년을 되돌려 돌리는 검증 도구, (2) `recommendation_history`에 관측 특성을
  남기는 기록 경로(`higher_low` 추가 — 점수에는 안 들어간다).

  **남은 숙제**: "점수 상위가 오히려 나쁘다"(1~5위 중간값 0.00% vs 11위 이하
  12.66%)는 만점 지점으로도 보너스 제거로도 고쳐지지 않았다. 점수 구성 요소가
  성과와 단조 관계가 아니라서 생기는 문제이고, 선발 방식 자체를 바꾸는 별도
  설계가 필요하다.

  ⚠️ 이 근거들은 2024~2025년 표본의 **구간 간 상대 비교**다. 유니버스가 오늘
  시점 목록이라 상장폐지 종목이 빠져 있어(생존 편향) 절대 수익률은 믿을 수 없다.

v6 (2026-09-14, **거래량 점수를 산 모양으로 — 방향이 거꾸로였다**):

  v5가 실패하면서 얻은 표가 근거다. 거래량 가중치는 0.3인데 지금까지 구간 표를
  찍은 적이 없었고, 처음 찍어 보니 **점수를 많이 줄수록 성적이 나빴다.**
  수치는 `_vol_score_val` 주석에 그대로 있다.

  옛 공식 `min(1, (vol_ratio-1)/2)`는 3.0배에서 만점이었다. 그런데 가장 좋은 구간은
  1.0~1.5배(중간값 15.93%·승률 62.7%)이고 1.5배 위는 2.68%/2.83%로 무너진다.
  **점수의 30%가 반대 방향을 가리키고 있었다.**

  높은 점수를 받으려면 거래량비가 높아야 하는데 높은 거래량비가 나쁜 성과를 예고하니,
  "점수 상위가 오히려 나쁘다"가 그대로 설명된다 — v4·v5에서 못 풀던 숙제의 가장
  유력한 원인이다.

  **v5의 교훈을 그대로 적용한 것이기도 하다.** v5는 "소진일수에 자를 진짜 경계가
  없어서" 실패했다. 거래량비는 1.5배를 경계로 15.93 → 2.68로 뚝 떨어져 **경계가
  실재한다.** 종목수도 84~150개로 넉넉하고 쏠림도 2.1~4.3%로 낮다.

  **미리 정한 합격 기준** (v5와 동일, 기준선도 v4 원본 run 34809895549 · 694건):
    (1) 점수 구간별 중간값이 **단조 증가**할 것 — 지금 3.46 / 11.06 / 20.77 / **-3.09**
    (2) 순위 1~5위가 11위 이하보다 **나쁘지 않을 것** — 지금 0.00 vs **12.66**
  하나라도 안 되면 실패로 보고 되돌린다.

  ⚠️ 이번엔 하드 필터가 아니라 **가산점(채점) 변경**이라 선발 구성이 바뀐다.
  v4에서 네 번 당한 함정이 그대로 적용된다 — 관측이 재현되지 않을 수 있다.
  다만 이번 관측은 "조건 A가 좋더라"가 아니라 **"점수 공식이 성과와 반대로 간다"**는
  구조적 오류라, 고치는 방향 자체는 표본이 바뀌어도 유지된다고 본다.

  **v6 결과 (run 34857002220, 758건) — 절반의 성공.**

    기준                        원본            v6          판정
    순위 1~5위 중간값/승률      0.00 / 49.6   10.74 / 62.6   ✓ 통과
    순위 11위 이하              12.66 / 63.1  10.09 / 59.4
    점수 구간 단조 증가          -              60~69점 1.72  ✗ 실패

  **순위 역전이 완전히 풀렸다** — 1~5위(10.74) > 6~10위(10.49) > 11위 이하(10.09).
  v4·v5에서 못 풀던 핵심 문제가 해결됐다. 의도한 효과도 나왔다: 거래량 1.5~2.0배가
  94 → 69건, 2.0배 이상이 116 → 21건으로 걸러졌고 그 구간 성적은 -5.56% / -3.09%다.

  ⚠️ 대신 **점수 눈금이 무너졌다** — 70점 이상이 758건 중 537건(71%)이 됐다.
  거래량 점수가 예전엔 대부분 0점 근처였는데 이제 대부분 만점 근처라, 모두의 점수가
  통째로 위로 밀렸기 때문이다.

  ⚠️ **합격 기준 (1)이 잘못 설계돼 있었다.** "40~49점", "70점 이상" 같은 **절대 점수
  칸**을 비교하는데, 눈금 자체가 이동하면 그 칸은 더 이상 같은 뜻이 아니다. 같은
  질문을 눈금과 무관하게 묻는 것이 기준 (2)(순위)이고 그건 통과했다. 기준을 나중에
  바꾼 것이 아니라 **처음부터 틀린 기준이었음을 기록해 둔다.**

v7 (2026-09-14, **가중치를 변별력 있는 조건으로 옮긴다**):

  v6이 남긴 눈금 문제를 푼다. 원인은 거래량이 아니라 더 깊은 곳에 있었다 —
  **세 조건 중 둘이 거의 상수다.**

    조건        가중치   만점 비율            성과와의 관계
    소진일수      0.4    75% (90일+ 449건)   30일 위로 평평 (v5에서 확인)
    거래량        0.3    88% (1.5배 미만)    산 모양으로 고쳤으나 대부분 상단
    하락률        0.3    거의 없음            **단조 증가** (아래)

  점수 ≈ (상수 0.67) + 0.3 × 하락률점수가 돼 있었다. 71%가 70점을 넘은 것도,
  그런데도 순위가 제대로 선 것도 같은 이유다 — 실질적으로 하락률만 줄을 세우고 있었다.

  **하락률은 두 측정이 모두 지지하는 유일한 조건이다.**
    스윕(선발 없음, 10,456건): 25~35% 3.18 → 55~65% 13.62 → 75~85% 17.24 → 85%+ 22.51
    본 분석(선발 있음, 758건):  55~65% 9.80 → 65~75% 11.06 → 75%+ 14.74
  선발 효과가 없는 쪽과 있는 쪽이 **같은 방향**을 가리킨다. 이번 작업에서 이런 조건은
  하락률뿐이다(v4의 `MIN_DRAWDOWN` 55% 계단도 같은 근거였다).

  그래서 소진일수 0.4 → 0.2, 그 0.2를 하락률로 옮겨 0.3 → 0.5로 한다.
  거래량 0.3은 그대로 둔다(v6에서 막 고쳤으므로 한 번에 하나씩).

  **합격 기준 — (1)을 고쳐 다시 적는다** (기준선: v6 run 34857002220, 758건):
    (1) **분포가 다시 펼쳐질 것** — 어느 점수 구간도 표본의 50%를 넘지 않을 것
        (지금 70점 이상이 71%)
    (2) 순위 1~5위가 11위 이하보다 **나쁘지 않을 것** — v6에서 얻은 10.74 vs 10.09를
        지킬 것
    (3) (1)이 충족된 뒤에야 의미가 있다 — 점수 구간 중간값이 **단조 증가**할 것
  (1)이나 (2)가 깨지면 실패로 보고 되돌린다.

  **결과 (run 34865967278, 744건) — 통과.** 세 단계 만에 처음이다.

    기준                          v6            v7           판정
    최대 점수구간 비중            71%          34.9%          ✓ 통과
    순위 1~5위 중간값          10.74         11.59           ✓ 통과
    순위 11위 이하             10.09          9.88

    점수 구간      원본 중간값/승률    v7 중간값/승률
    40~49점         3.46 / 52.0       5.51 / 60.0
    50~59점        11.06 / 61.9       3.34 / 52.6
    60~69점        20.77 / 68.1      13.43 / 62.7
    70점 이상      -3.09 / 48.3      19.53 / 64.7   ← 꼴찌에서 1등으로

    순위           원본              v7
    1~5위           0.00 / 49.6     11.59 / 58.7
    6~10위          2.19 / 50.9     14.29 / 59.7
    11위 이하      12.66 / 63.1      9.88 / 60.1

  **점수가 가장 높은 구간이 이제 가장 좋다.** v4부터 끌고 온 "점수 상위가 오히려
  나쁘다"는 숙제가 v6(방향) + v7(가중치) 두 단계로 해결됐다.

  ⚠️ 남는 흠 세 가지를 숨기지 않고 적는다.
    - 완전한 단조 증가는 아니다 — 50~59점(3.34%)이 40~49점(5.51%)보다 낮다.
      (기준 (3)은 처음부터 되돌림 조건으로 걸지 않았다.) 50점 위로는 확실히 오른다.
    - 70점 이상 구간의 최다종목 비중이 5.9%로 다른 구간(1.9~3.1%)보다 높다.
    - 생존 편향은 그대로다. 절대 수익률이 아니라 **구간 간 상대 비교**로만 읽을 것.

  **세 단계를 관통한 교훈**: 조건 하나하나가 "좋은가"를 묻지 말고,
  **점수 공식이 성과와 같은 방향으로 가는가**를 물을 것. v4에서 네 번 실패한 것은
  전부 전자였고, 성공한 v6·v7은 후자였다.

v8 (2026-09-15, **거래량 점수를 계단 하나로 — 가정을 걷어낸다**):

  v7을 병합하고 남긴 숙제 두 가지를 진단하려고 **채점은 손대지 않은 채** 측정 구간만
  잘게 쪼개 재생했다(run 34918005456, 746건).

  **숙제 2 답 — 정점은 내가 잡은 자리가 아니었다.** 수치는 `_vol_score_val` 주석에
  그대로 있다. 1.5배 아래로는 뚜렷한 정점이 없고(9.77~16.05를 오르내릴 뿐), 실제
  1등은 만점을 못 받던 0.85~1.0배였다(16.05%·승률 66.9%). v6~v7의 기울기는
  측정값이 아니라 **가정**이었고, 그 가정이 틀렸다. 걷어내고 1.5배 미만을 전부
  만점으로 둔다.

  **숙제 1은 좁혀졌지만 지금 건드리지 않는다.** "50~59점이 나쁘다"가 아니라
  **55~59점 한 칸**이었다:

        점수구간     n   중간값    승률   -30%이하
        40~44점     28   13.36%  67.9%     7.1
        45~49점     40    6.09%  57.5%    15.0
        50~54점    114    8.49%  57.9%     6.1
        55~59점    118   -2.26%  47.5%    22.0   ← 여기 한 칸
        60~64점    136   13.85%  61.8%     7.4
        65~69점    128   11.68%  62.5%    12.5
        70~79점    129   17.75%  63.6%    10.1
        80점 이상   53   25.43%  67.9%     9.4

  크게 깨진 비율(-30% 이하)이 22.0%로 다른 구간의 두 배다. **왜인지는 모른다.**
  그리고 이번 v8이 표본의 47%에게 점수를 더 주므로 분포가 통째로 밀린다 —
  "55~59점"이라는 칸 자체가 다른 종목으로 채워져 지금 진단이 무의미해진다.
  **v8 이후에 다시 본다.**

  **미리 정한 합격 기준** (기준선: v7 run 34918005456, 746건):
    (1) 어느 점수 구간도 표본의 50%를 넘지 않을 것 (지금 최대 35.4%)
    (2) 순위 1~5위가 11위 이하보다 나쁘지 않을 것 (지금 10.74 vs 10.38)
  하나라도 깨지면 실패로 보고 되돌린다.

  **결과 (run 34919096544, 747건) — 기준은 통과, 성적은 제자리.**

    기준                          v7            v8          판정
    최대 점수구간 비중           35.4%        37.3%          ✓ 통과
    순위 1~5위 중간값           10.74        10.72          ✓ 통과
    순위 11위 이하              10.38         9.71

  **의도한 효과는 나왔다**: 2.0배 이상이 21건 → **0건**으로 완전히 걸러졌고,
  실제 1등인 0.85~1.0배가 이제 만점을 받는다(16.10%). 0.70~0.85배는 171 → 279건으로
  늘었고 성적도 12.89%로 괜찮다.

  ⚠️ **그러나 성적이 좋아지지는 않았다.** 60~69점 13.04 → 6.79, 70점+ 20.15 → 17.24,
  순위 6~10위 13.90 → 9.72, 2024년 6.58 → 5.41. 기준은 "망가뜨리지 말 것"이었지
  "좋아질 것"이 아니었고, 실제로 아무것도 좋아지지 않았다.
  **유지하는 근거는 "성적이 올라서"가 아니라 "측정 안 한 가정이 코드에서 빠져서"다.**

  **이번 실행이 남긴 진짜 소득 — 남은 울퉁불퉁함의 정체가 드러났다.**

  거래량이 92%에게 만점을 주게 되면서 점수가 사실상 **하락률 순위 그 자체**가 됐다:

      점수 ≈ 0.5(거래량·소진일수가 거의 상수) + 0.5 × 하락률점수

  그러자 하락률 곡선의 모양이 점수에 그대로 비쳤다. 선발 없는 스윕(10,463건, 60거래일):

      55~65%  13.51%
      65~75%   9.64%   ← 골
      75~85%  17.70%
      85% 이상 21.37%

  **하락률 자체가 65~75%에서 한 번 꺼진다.** 60~69점 구간(≈ 하락률 65~75%)이
  6.79%로 꺼진 것이 이것으로 보인다. v7까지 "하락률은 단조 증가"로 본 것은
  거친 구간(10%폭 셋)에서만 맞았고, 실제 곡선에는 골이 있다.

  즉 남은 문제는 거래량이나 가중치가 아니라 **하락률 점수의 모양**이다.
  55~59점 칸도 여전히 마이너스다(-1.54%, 94건 — v7의 -2.26%/118건에서 줄었을 뿐).
  다음에 손댄다면 여기다.

v5 (2026-09-14, **소진일수 커트라인 15 → 30일 — 시도했다가 되돌림**):

  v4에서 네 가지가 전부 기각된 뒤, "점수가 높을수록 좋아야 하는데 상위권이 오히려
  나쁘다"는 숙제를 한 번에 하나씩 풀기로 한 첫 시도다. **실패했다.**

  **가설** — 15~29일이 694건 중 유일한 마이너스 구간이었다(중간값 -3.96%·승률 44.0%,
  50건, 쏠림 6.0%). 30일을 넘으면 12.44 → 12.25 → 10.43으로 평평하므로 가산점이
  아니라 커트라인의 성격이라고 봤다. v4에서 살아남은 유일한 변경(`MIN_DRAWDOWN`)과
  같은 하드 필터 형태라 성공 확률이 높다고 판단했다.

  **결과 (run 34827183040, 678건)** — 미리 정한 합격 기준 두 개가 모두 실패했다.
    (1) 점수 구간 단조 증가 → 70점 이상이 **-3.09% 그대로**
    (2) 순위 1~5위가 나빠지지 않을 것 → 0.00% → **-1.52%**, 승률 49.6% → 48.6%
  전체는 제자리였다(중간값 10.32 → 10.43, 승률 58.6 → 58.8).

  **왜 안 먹혔나 — 하드 필터도 선발을 바꾼다.**
  잘라낸 건 50건인데 표본은 694 → 678로 **16건만 줄었다.** 34건이 빈자리를 메우고
  상위 20에 새로 들어왔다는 뜻이다. 그리고 바로 위 구간이 무너졌다 —
  30~44일 중간값 12.44% → **0.99%**. 나쁜 구간을 쳐내자 그 자리를 다른 나쁜 종목이
  채웠다. **두더지 잡기였다.**

  ⚠️ v4의 교훈을 한 겹 더 정확하게 고쳐 적는다. "하드 필터는 안전하다"가 아니다 —
  `MIN_DRAWDOWN`(55%)이 통한 건 하드 필터여서가 아니라 **하락률 자체가 성과와
  단조 관계였기 때문**이다(스윕으로 선발 없이 측정해 확인했다). 소진일수는 30일 위로
  평평해서 잘라낼 '진짜 경계'가 없었고, 그래서 자른 자리가 그대로 메워졌다.
  **필터든 가산점이든, 그 지표가 성과와 단조 관계일 때만 통한다.**

  **이 시도가 남긴 진짜 소득 — 거래량 점수가 거꾸로다.**
  이번에 처음으로 거래량비 구간 표를 찍었다(가중치가 0.3인데 지금까지 측정한 적이
  없었다):

      거래량비        점수     n   중간값    승률   종목수  최다종목%
      1.0배 미만      0점    275   11.06%  61.8%    150      2.2
      1.0~1.5배    0~25점   193   15.93%  62.7%    131      2.1
      1.5~2.0배   25~50점    94    2.68%  51.1%     84      4.3
      2.0배 이상  50~100점   116    2.83%  51.7%     87      3.4

  **점수를 많이 줄수록 성적이 나쁘다.** 가장 좋은 구간(1.0~1.5배)은 점수를 거의 못 받고,
  점수를 가장 많이 받는 구간(2.0배 이상)이 꼴찌다. 종목수가 많고(84~150) 쏠림이
  낮아(2.1~4.3%) 특정 종목이 만든 숫자도 아니다.

  가중치 0.3이 **반대 방향을 가리키고 있다.** 점수 상위가 나쁜 이유로 지금까지 나온
  설명 중 가장 잘 들어맞는다 — 높은 점수를 받으려면 거래량비가 높아야 하는데, 높은
  거래량비가 나쁜 성과를 예고하기 때문이다. v6에서 다룬다.

  ⚠️ 이 근거들은 2024~2025년 표본의 **구간 간 상대 비교**다. 유니버스가 오늘
  시점 목록이라 상장폐지 종목이 빠져 있어(생존 편향) 절대 수익률은 믿을 수 없다.

v6 (2026-09-14, **거래량 점수를 산 모양으로 — 방향이 거꾸로였다**):

  v5가 실패하면서 얻은 표가 근거다. 거래량 가중치는 0.3인데 지금까지 구간 표를
  찍은 적이 없었고, 처음 찍어 보니 **점수를 많이 줄수록 성적이 나빴다.**
  수치는 `_vol_score_val` 주석에 그대로 있다.

  옛 공식 `min(1, (vol_ratio-1)/2)`는 3.0배에서 만점이었다. 그런데 가장 좋은 구간은
  1.0~1.5배(중간값 15.93%·승률 62.7%)이고 1.5배 위는 2.68%/2.83%로 무너진다.
  **점수의 30%가 반대 방향을 가리키고 있었다.**

  높은 점수를 받으려면 거래량비가 높아야 하는데 높은 거래량비가 나쁜 성과를 예고하니,
  "점수 상위가 오히려 나쁘다"가 그대로 설명된다 — v4·v5에서 못 풀던 숙제의 가장
  유력한 원인이다.

  **v5의 교훈을 그대로 적용한 것이기도 하다.** v5는 "소진일수에 자를 진짜 경계가
  없어서" 실패했다. 거래량비는 1.5배를 경계로 15.93 → 2.68로 뚝 떨어져 **경계가
  실재한다.** 종목수도 84~150개로 넉넉하고 쏠림도 2.1~4.3%로 낮다.

  **미리 정한 합격 기준** (v5와 동일, 기준선도 v4 원본 run 34809895549 · 694건):
    (1) 점수 구간별 중간값이 **단조 증가**할 것 — 지금 3.46 / 11.06 / 20.77 / **-3.09**
    (2) 순위 1~5위가 11위 이하보다 **나쁘지 않을 것** — 지금 0.00 vs **12.66**
  하나라도 안 되면 실패로 보고 되돌린다.

  ⚠️ 이번엔 하드 필터가 아니라 **가산점(채점) 변경**이라 선발 구성이 바뀐다.
  v4에서 네 번 당한 함정이 그대로 적용된다 — 관측이 재현되지 않을 수 있다.
  다만 이번 관측은 "조건 A가 좋더라"가 아니라 **"점수 공식이 성과와 반대로 간다"**는
  구조적 오류라, 고치는 방향 자체는 표본이 바뀌어도 유지된다고 본다.

  **v6 결과 (run 34857002220, 758건) — 절반의 성공.**

    기준                        원본            v6          판정
    순위 1~5위 중간값/승률      0.00 / 49.6   10.74 / 62.6   ✓ 통과
    순위 11위 이하              12.66 / 63.1  10.09 / 59.4
    점수 구간 단조 증가          -              60~69점 1.72  ✗ 실패

  **순위 역전이 완전히 풀렸다** — 1~5위(10.74) > 6~10위(10.49) > 11위 이하(10.09).
  v4·v5에서 못 풀던 핵심 문제가 해결됐다. 의도한 효과도 나왔다: 거래량 1.5~2.0배가
  94 → 69건, 2.0배 이상이 116 → 21건으로 걸러졌고 그 구간 성적은 -5.56% / -3.09%다.

  ⚠️ 대신 **점수 눈금이 무너졌다** — 70점 이상이 758건 중 537건(71%)이 됐다.
  거래량 점수가 예전엔 대부분 0점 근처였는데 이제 대부분 만점 근처라, 모두의 점수가
  통째로 위로 밀렸기 때문이다.

  ⚠️ **합격 기준 (1)이 잘못 설계돼 있었다.** "40~49점", "70점 이상" 같은 **절대 점수
  칸**을 비교하는데, 눈금 자체가 이동하면 그 칸은 더 이상 같은 뜻이 아니다. 같은
  질문을 눈금과 무관하게 묻는 것이 기준 (2)(순위)이고 그건 통과했다. 기준을 나중에
  바꾼 것이 아니라 **처음부터 틀린 기준이었음을 기록해 둔다.**

v7 (2026-09-14, **가중치를 변별력 있는 조건으로 옮긴다**):

  v6이 남긴 눈금 문제를 푼다. 원인은 거래량이 아니라 더 깊은 곳에 있었다 —
  **세 조건 중 둘이 거의 상수다.**

    조건        가중치   만점 비율            성과와의 관계
    소진일수      0.4    75% (90일+ 449건)   30일 위로 평평 (v5에서 확인)
    거래량        0.3    88% (1.5배 미만)    산 모양으로 고쳤으나 대부분 상단
    하락률        0.3    거의 없음            **단조 증가** (아래)

  점수 ≈ (상수 0.67) + 0.3 × 하락률점수가 돼 있었다. 71%가 70점을 넘은 것도,
  그런데도 순위가 제대로 선 것도 같은 이유다 — 실질적으로 하락률만 줄을 세우고 있었다.

  **하락률은 두 측정이 모두 지지하는 유일한 조건이다.**
    스윕(선발 없음, 10,456건): 25~35% 3.18 → 55~65% 13.62 → 75~85% 17.24 → 85%+ 22.51
    본 분석(선발 있음, 758건):  55~65% 9.80 → 65~75% 11.06 → 75%+ 14.74
  선발 효과가 없는 쪽과 있는 쪽이 **같은 방향**을 가리킨다. 이번 작업에서 이런 조건은
  하락률뿐이다(v4의 `MIN_DRAWDOWN` 55% 계단도 같은 근거였다).

  그래서 소진일수 0.4 → 0.2, 그 0.2를 하락률로 옮겨 0.3 → 0.5로 한다.
  거래량 0.3은 그대로 둔다(v6에서 막 고쳤으므로 한 번에 하나씩).

  **합격 기준 — (1)을 고쳐 다시 적는다** (기준선: v6 run 34857002220, 758건):
    (1) **분포가 다시 펼쳐질 것** — 어느 점수 구간도 표본의 50%를 넘지 않을 것
        (지금 70점 이상이 71%)
    (2) 순위 1~5위가 11위 이하보다 **나쁘지 않을 것** — v6에서 얻은 10.74 vs 10.09를
        지킬 것
    (3) (1)이 충족된 뒤에야 의미가 있다 — 점수 구간 중간값이 **단조 증가**할 것
  (1)이나 (2)가 깨지면 실패로 보고 되돌린다.

  **결과 (run 34865967278, 744건) — 통과.** 세 단계 만에 처음이다.

    기준                          v6            v7           판정
    최대 점수구간 비중            71%          34.9%          ✓ 통과
    순위 1~5위 중간값          10.74         11.59           ✓ 통과
    순위 11위 이하             10.09          9.88

    점수 구간      원본 중간값/승률    v7 중간값/승률
    40~49점         3.46 / 52.0       5.51 / 60.0
    50~59점        11.06 / 61.9       3.34 / 52.6
    60~69점        20.77 / 68.1      13.43 / 62.7
    70점 이상      -3.09 / 48.3      19.53 / 64.7   ← 꼴찌에서 1등으로

    순위           원본              v7
    1~5위           0.00 / 49.6     11.59 / 58.7
    6~10위          2.19 / 50.9     14.29 / 59.7
    11위 이하      12.66 / 63.1      9.88 / 60.1

  **점수가 가장 높은 구간이 이제 가장 좋다.** v4부터 끌고 온 "점수 상위가 오히려
  나쁘다"는 숙제가 v6(방향) + v7(가중치) 두 단계로 해결됐다.

  ⚠️ 남는 흠 세 가지를 숨기지 않고 적는다.
    - 완전한 단조 증가는 아니다 — 50~59점(3.34%)이 40~49점(5.51%)보다 낮다.
      (기준 (3)은 처음부터 되돌림 조건으로 걸지 않았다.) 50점 위로는 확실히 오른다.
    - 70점 이상 구간의 최다종목 비중이 5.9%로 다른 구간(1.9~3.1%)보다 높다.
    - 생존 편향은 그대로다. 절대 수익률이 아니라 **구간 간 상대 비교**로만 읽을 것.

  **세 단계를 관통한 교훈**: 조건 하나하나가 "좋은가"를 묻지 말고,
  **점수 공식이 성과와 같은 방향으로 가는가**를 물을 것. v4에서 네 번 실패한 것은
  전부 전자였고, 성공한 v6·v7은 후자였다.

v8 (2026-09-15, **거래량 점수를 계단 하나로 — 가정을 걷어낸다**):

  v7을 병합하고 남긴 숙제 두 가지를 진단하려고 **채점은 손대지 않은 채** 측정 구간만
  잘게 쪼개 재생했다(run 34918005456, 746건).

  **숙제 2 답 — 정점은 내가 잡은 자리가 아니었다.** 수치는 `_vol_score_val` 주석에
  그대로 있다. 1.5배 아래로는 뚜렷한 정점이 없고(9.77~16.05를 오르내릴 뿐), 실제
  1등은 만점을 못 받던 0.85~1.0배였다(16.05%·승률 66.9%). v6~v7의 기울기는
  측정값이 아니라 **가정**이었고, 그 가정이 틀렸다. 걷어내고 1.5배 미만을 전부
  만점으로 둔다.

  **숙제 1은 좁혀졌지만 지금 건드리지 않는다.** "50~59점이 나쁘다"가 아니라
  **55~59점 한 칸**이었다:

        점수구간     n   중간값    승률   -30%이하
        40~44점     28   13.36%  67.9%     7.1
        45~49점     40    6.09%  57.5%    15.0
        50~54점    114    8.49%  57.9%     6.1
        55~59점    118   -2.26%  47.5%    22.0   ← 여기 한 칸
        60~64점    136   13.85%  61.8%     7.4
        65~69점    128   11.68%  62.5%    12.5
        70~79점    129   17.75%  63.6%    10.1
        80점 이상   53   25.43%  67.9%     9.4

  크게 깨진 비율(-30% 이하)이 22.0%로 다른 구간의 두 배다. **왜인지는 모른다.**
  그리고 이번 v8이 표본의 47%에게 점수를 더 주므로 분포가 통째로 밀린다 —
  "55~59점"이라는 칸 자체가 다른 종목으로 채워져 지금 진단이 무의미해진다.
  **v8 이후에 다시 본다.**

  **미리 정한 합격 기준** (기준선: v7 run 34918005456, 746건):
    (1) 어느 점수 구간도 표본의 50%를 넘지 않을 것 (지금 최대 35.4%)
    (2) 순위 1~5위가 11위 이하보다 나쁘지 않을 것 (지금 10.74 vs 10.38)
  하나라도 깨지면 실패로 보고 되돌린다.

  **결과 (run 34919096544, 747건) — 기준은 통과, 성적은 제자리.**

    기준                          v7            v8          판정
    최대 점수구간 비중           35.4%        37.3%          ✓ 통과
    순위 1~5위 중간값           10.74        10.72          ✓ 통과
    순위 11위 이하              10.38         9.71

  **의도한 효과는 나왔다**: 2.0배 이상이 21건 → **0건**으로 완전히 걸러졌고,
  실제 1등인 0.85~1.0배가 이제 만점을 받는다(16.10%). 0.70~0.85배는 171 → 279건으로
  늘었고 성적도 12.89%로 괜찮다.

  ⚠️ **그러나 성적이 좋아지지는 않았다.** 60~69점 13.04 → 6.79, 70점+ 20.15 → 17.24,
  순위 6~10위 13.90 → 9.72, 2024년 6.58 → 5.41. 기준은 "망가뜨리지 말 것"이었지
  "좋아질 것"이 아니었고, 실제로 아무것도 좋아지지 않았다.
  **유지하는 근거는 "성적이 올라서"가 아니라 "측정 안 한 가정이 코드에서 빠져서"다.**

  **이번 실행이 남긴 진짜 소득 — 남은 울퉁불퉁함의 정체가 드러났다.**

  거래량이 92%에게 만점을 주게 되면서 점수가 사실상 **하락률 순위 그 자체**가 됐다:

      점수 ≈ 0.5(거래량·소진일수가 거의 상수) + 0.5 × 하락률점수

  그러자 하락률 곡선의 모양이 점수에 그대로 비쳤다. 선발 없는 스윕(10,463건, 60거래일):

      55~65%  13.51%
      65~75%   9.64%   ← 골
      75~85%  17.70%
      85% 이상 21.37%

  **하락률 자체가 65~75%에서 한 번 꺼진다.** 60~69점 구간(≈ 하락률 65~75%)이
  6.79%로 꺼진 것이 이것으로 보인다. v7까지 "하락률은 단조 증가"로 본 것은
  거친 구간(10%폭 셋)에서만 맞았고, 실제 곡선에는 골이 있다.

  즉 남은 문제는 거래량이나 가중치가 아니라 **하락률 점수의 모양**이다.
  55~59점 칸도 여전히 마이너스다(-1.54%, 94건 — v7의 -2.26%/118건에서 줄었을 뿐).
  다음에 손댄다면 여기다.
"""
from __future__ import annotations

from typing import Any

import numpy as np
import pandas as pd

# ── 튜닝 상수 (여기서 직접 조정) ────────────────────────────────────
TOP_N = 20
MIN_SCORE = 0.40

# 가격 범위
MIN_PRICE = 0.50
MAX_PRICE = 50.0

# 하드 필터 기준값
MIN_DRAWDOWN = 0.55          # 52주 최고가 대비 55% 이상 하락
# 저점 갱신 중단 기간 커트라인. **30으로 올려봤다가 되돌렸다** (v5, docstring 참고).
MIN_DAYS_SINCE_LOW = 15
MIN_VOL_RATIO = 0.70         # 최근 20일 거래량 / 직전 40일 거래량 ≥ 0.70 (유지)

# 거래량 점수는 **계단 하나**다 (v8, 2026-09-15). 1.5배가 유일한 진짜 경계이고,
# 그 아래에서는 성적에 뚜렷한 순서가 없다 — 없는 신호를 지어내지 않는다(docstring v8).
VOL_PEAK_HIGH = 1.5          # 여기까지 전부 만점
VOL_FADE_END = 2.0           # 여기서 0점 (1.5~2.0 중간값 -6.26%, 2.0배 이상 -9.25%)
MIN_DOLLAR_VOL = 300_000     # 일평균 거래대금 $30만 이상 (최소 유동성)

# 복합 점수 가중치 (합산 = 1.0)
# **가중치는 "변별력이 있는 조건"에 준다** (v7, 2026-09-14). 근거는 docstring v7.
# 소진일수는 표본의 75%가 만점이라 0.4를 받고도 사실상 상수였다. 하락률은 선발과
# 무관한 스윕에서도, 선발된 표본 안에서도 성과와 같이 움직이는 **유일한** 조건이다.
WEIGHT_DRAWDOWN = 0.5        # v7에서 0.3 → 0.5 (변별력이 확인된 유일한 조건)
WEIGHT_EXHAUSTION = 0.2      # v7에서 0.4 → 0.2 (75%가 만점이라 줄을 못 세운다)
WEIGHT_VOLUME = 0.3          # 그대로 (v6에서 방향을 고쳤다)

# 점수 만점 지점. **당겨봤다가 되돌린 값이다** — 근거는 모듈 docstring v4 ①.
# 65%/50일로 당기면 대부분이 만점을 받아 점수가 줄을 못 세운다(70점 이상이 표본의
# 65%가 됐다). 넓게 두는 것이 변별력을 지킨다. 값은 v3와 같고, 이름만 붙여
# "어디가 만점인지"가 코드에서 바로 보이게 했다.
DRAWDOWN_FULL_SPAN = 0.35    # 하한(55%) + 이만큼 = 90%에서 하락률 만점
EXHAUSTION_FULL_SPAN = 45.0  # 하한(15일) + 이만큼 = 60일에서 소진일수 만점

# 보너스 점수 (최종 점수는 1.0으로 상한)
VCP_ATR_SHORT = 10           # 단기 ATR 기간
VCP_ATR_LONG = 50            # 장기 ATR 기간
VCP_ATR_THRESHOLD = 0.6      # ATR_SHORT / ATR_LONG ≤ 이 값 → 변동성 수축
VCP_BONUS = 0.10

MA_SHORT1 = 5
MA_SHORT2 = 10
MA_SHORT3 = 20
MA_ALIGN_BONUS = 0.10        # 현재가 > SMA5 > SMA10 > SMA20

# 저점 높이기는 **점수에 넣지 않고 기록만 한다** (2026-09-14 시도했다가 되돌림).
# 관측으로는 우위가 뚜렷했지만 가산점을 주니 선발 구성이 바뀌어 우위가 사라졌다.
HIGHER_LOW_SPAN = 20         # 비교 구간(거래일). supportSignals.ts와 같은 길이

VOL_TRIGGER_MULTIPLIER = 2.0  # 오늘 거래량이 90일 평균 2배 이상 → 거래량 트리거
DOJI_BODY_RATIO = 0.1         # 몸통 ÷ (고가-저가) 이 값 이하면 십자형(도지)로 본다


# ── 기술 지표 (NumPy 벡터 연산) ─────────────────────────────────────

def _true_range(high: np.ndarray, low: np.ndarray, close: np.ndarray) -> np.ndarray:
    prev_close = np.empty_like(close)
    prev_close[0] = close[0]
    prev_close[1:] = close[:-1]
    return np.maximum(
        high - low,
        np.maximum(np.abs(high - prev_close), np.abs(low - prev_close)),
    )


def _vcp_bonus_val(high: np.ndarray, low: np.ndarray, close: np.ndarray) -> float:
    if len(close) < VCP_ATR_LONG + 1:
        return 0.0
    tr = _true_range(high, low, close)
    atr_short = tr[-VCP_ATR_SHORT:].mean()
    atr_long = tr[-VCP_ATR_LONG:].mean()
    return VCP_BONUS if (atr_long > 0 and atr_short / atr_long <= VCP_ATR_THRESHOLD) else 0.0


def _vol_score_val(vol_ratio: float) -> float:
    """거래량비 → 0~1 점수. **1.5배를 경계로 한 계단 하나다** (v8, 2026-09-15).

    거래량비를 정점 주변에서 잘게 쪼개 재생한 결과(746건, 60거래일):

        거래량비        n   중간값    승률   종목수  최다종목%
        0.70~0.85배   171   11.83%  64.9%    103      2.9
        0.85~1.0배    178   16.05%  66.9%    126      2.8   ← 실제 1등
        1.0~1.25배    193    9.77%  58.0%    126      2.1
        1.25~1.5배    109   14.95%  62.4%     87      2.8
        1.5~2.0배      74   -6.26%  36.5%     60      5.4
        2.0배 이상      21   -9.25%  42.9%     15     14.3

    **1.5배 아래로는 뚜렷한 정점이 없다.** 9.77~16.05 사이를 오르내릴 뿐 순서에 규칙이
    없다. 진짜 경계는 1.5배 하나이고, 그 위에서만 마이너스로 무너진다.

    v6~v7은 1.0~1.5배를 정점으로 놓고 그 아래를 0.5점부터 선형으로 깎았는데, 그
    기울기는 **측정한 값이 아니라 가정이었다.** 실제로는 0.85~1.0배가 1등(16.05%)인데도
    만점을 못 받고 있었다. 가정을 걷어내고 데이터가 말하는 것만 남긴다:

        1.5배 미만 → 만점,  1.5~2.0배 → 선형 감소,  2.0배 이상 → 0점

    ⚠️ **거래량은 줄 세우는 조건이 아니라 걸러내는 조건이다.** 1.5배 아래에서 순위를
    매기면 없는 신호를 지어내는 셈이다. 그래서 그 구간을 평평하게 둔다 —
    변별은 하락률(가중치 0.5)이 맡는다.
    """
    if vol_ratio < VOL_PEAK_HIGH:
        return 1.0
    fade = (vol_ratio - VOL_PEAK_HIGH) / (VOL_FADE_END - VOL_PEAK_HIGH)
    return max(0.0, 1.0 - fade)


def _ma_align_bonus_val(close: np.ndarray) -> float:
    """단기 이평 정배열이면 +0.10.

    **2026-09-14에 빼봤다가 되돌렸다.** 관측만 보면 정배열 종목이 오히려 나빴지만
    (중간값 5.86%·승률 56.5% vs 10.84%·59.3%), 실제로 빼고 재검증하니 전체가
    나아지지 않았고 다른 조건의 관측값까지 뒤집혔다. 자세한 수치는 docstring v4 ③.
    """
    if len(close) < MA_SHORT3:
        return 0.0
    sma5 = close[-MA_SHORT1:].mean()
    sma10 = close[-MA_SHORT2:].mean()
    sma20 = close[-MA_SHORT3:].mean()
    return MA_ALIGN_BONUS if (close[-1] > sma5 > sma10 > sma20) else 0.0


def _is_higher_low(low: np.ndarray) -> bool:
    """최근 20봉 저점이 그 직전 20봉 저점보다 높은가. **점수에는 안 들어간다.**

    저점을 안 깨는 기간(`days_since_low`)만으로는 바닥을 기는 종목과 바닥을
    들어올리는 종목이 구분되지 않는다. 『매매의 기술』은 역헤드앤숄더를 "저점을
    높이며 거래량 증가"로 설명한다.

    **관측으로는 가장 뚜렷했는데 가산점으로는 실패했다** (2026-09-14):
    관측 단계에서는 60거래일 중간값 11.72%·승률 59.9% vs 8.62%·57.8%로 깨끗하게
    갈렸다. 그런데 +0.10을 주고 재검증하니 이 조건 종목이 272건 → 398건으로 늘면서
    중간값 8.36%·승률 56.8%로 떨어지고 승률은 오히려 역전됐다(안 높임 58.6%).
    가산점이 **선발 구성 자체를 바꿔서** 다른 조건이 약한 종목까지 상위에 올려보낸
    것으로 보인다.

    그래서 점수에서 빼고 `recommendation_history`에 기록만 남긴다 — 관측은 계속
    쌓되 선발에는 개입하지 않는다. 나중에 가산점이 아닌 **하드 필터**로 시험해 볼
    여지가 남아 있다.

    ⚠️ 교훈: "조건 A인 종목의 성적이 좋았다"는 "A에 가산점을 주면 전체가 좋아진다"를
    뜻하지 않는다. 가산점은 표본 자체를 바꾼다.
    """
    span = HIGHER_LOW_SPAN
    if len(low) < 2 * span:
        return False
    return bool(low[-span:].min() > low[-2 * span : -span].min())


def _is_volume_trigger_today(
    open_: np.ndarray,
    high: np.ndarray,
    low: np.ndarray,
    close: np.ndarray,
    vol: np.ndarray,
) -> bool:
    """오늘 터진 대량거래가 '매수' 쪽 신호인지 판정.

    거래량만으로는 방향을 알 수 없다 — 『매매의 기술』(2장 거래량):
    **"거래량은 타이밍만 제공한다. 방향은 봉의 모양이 결정한다."**
    같은 책의 매수 제3원칙(20일선 아래 급락 중)은 "거래량 증가 + **양봉 또는
    십자형**이 나올 때" 매수하라고 하고, 거래량 8원칙 2번은 "거래량 증가 +
    장대음봉 = **매물**"이라고 정반대로 못 박는다.

    예전에는 거래량 2배 조건만 보고 True를 돌려줬다. 그래서 악재로 투매가 터진 날
    (대량거래 장대음봉)도 반등 신호와 똑같이 ⚡ 배지가 붙어, 화면에서 정반대 신호가
    구분되지 않았다. 지금은 대량거래 **+ (양봉 또는 십자형)** 일 때만 True다.
    """
    if len(vol) < 2:
        return False
    n = min(90, len(vol) - 1)
    baseline = vol[-n - 1 : -1].mean()
    if not (baseline > 0 and vol[-1] >= baseline * VOL_TRIGGER_MULTIPLIER):
        return False

    o, h, l, c = float(open_[-1]), float(high[-1]), float(low[-1]), float(close[-1])
    if c > o:
        return True  # 양봉

    # 십자형(도지) = 대량거래인데 몸통이 거의 없다 → 황소와 곰이 치열하게 싸워
    # 우열이 안 갈린 상태. 책은 "큰 폭 하락 후 십자형"을 매수 신호로 본다.
    # 고가=저가(가격이 하루 내내 안 움직인 봉)는 싸움 자체가 없었다는 뜻이라 제외한다.
    bar_range = h - l
    if bar_range <= 0:
        return False
    return bool(abs(c - o) / bar_range <= DOJI_BODY_RATIO)


# ── 핵심 스코어링 ───────────────────────────────────────────────────

def _score_candidate(
    high: np.ndarray,
    low: np.ndarray,
    close: np.ndarray,
    vol: np.ndarray,
    high_52w: float,
) -> tuple[bool, dict]:
    """룰 기반 복합 스코어. 하드 필터 미충족 시 (False, {}) 반환."""
    if len(close) < 30:
        return False, {}

    current = float(close[-1])

    # 가격 범위
    if not (MIN_PRICE <= current <= MAX_PRICE):
        return False, {}

    # KIS 1년치 이력에서 계산된 52주 최고가 기준 하락률
    ref_high = high_52w if high_52w > 0 else float(high.max())
    drawdown = (ref_high - current) / ref_high if ref_high > 0 else 0.0
    if drawdown < MIN_DRAWDOWN:
        return False, {}

    # 저점 갱신 중단일 수 — reversed 배열에서 최소값 첫 인덱스 = 가장 최근 저점
    low_val = float(close.min())
    rev_indices = np.where(close[::-1] == low_val)[0]
    if len(rev_indices) == 0:
        return False, {}
    days_since_low = int(rev_indices[0])
    if days_since_low < MIN_DAYS_SINCE_LOW:
        return False, {}

    # 거래량 유지 비율
    n = len(vol)
    if n >= 61:
        recent_vol = vol[-20:].mean()
        baseline_vol = vol[-60:-20].mean()
    elif n >= 41:
        recent_vol = vol[-20:].mean()
        baseline_vol = vol[-40:-20].mean()
    else:
        half = max(n // 2, 1)
        recent_vol = vol[-half:].mean()
        baseline_vol = vol[:-half].mean() if n - half > 0 else vol[-1]
    vol_ratio = float(recent_vol / baseline_vol) if baseline_vol > 0 else 1.0
    if vol_ratio < MIN_VOL_RATIO:
        return False, {}

    # 복합 스코어
    drawdown_score = min(1.0, (drawdown - MIN_DRAWDOWN) / DRAWDOWN_FULL_SPAN)
    exhaustion_score = min(1.0, (days_since_low - MIN_DAYS_SINCE_LOW) / EXHAUSTION_FULL_SPAN)
    vol_score = _vol_score_val(vol_ratio)

    base = (
        WEIGHT_DRAWDOWN * drawdown_score
        + WEIGHT_EXHAUSTION * exhaustion_score
        + WEIGHT_VOLUME * vol_score
    )

    # 보너스는 VCP와 이평 정배열 둘뿐이다. 저점 높이기는 가산점으로 주면 선발
    # 구성이 바뀌어 역효과였으므로(docstring v4 ②) 채점에 넣지 않고 기록만 한다.
    vcp_b = _vcp_bonus_val(high, low, close)
    ma_b = _ma_align_bonus_val(close)
    score = min(1.0, base + vcp_b + ma_b)

    return True, {
        "score": score,
        "drawdown": drawdown,
        "days_since_low": days_since_low,
        "vol_ratio": vol_ratio,
        "vcp": vcp_b > 0,
        "ma_align": ma_b > 0,
        # 채점에 안 쓰이지만 남긴다 — 나중에 하드 필터로 다시 시험할 근거가 된다.
        "higher_low": _is_higher_low(low),
    }


# ── 메인 ─────────────────────────────────────────────────────────

def compute_pattern_matches(
    all_histories: dict[str, pd.DataFrame],
    universe: pd.DataFrame,
) -> list[dict[str, Any]]:
    """Gold Standard 바닥 특성에 부합하는 종목 상위 TOP_N 계산.

    Parameters
    ----------
    all_histories : 파이프라인이 수집한 전 종목 가격 이력 (ticker → DataFrame)
    universe      : US universe DataFrame (ticker, name, sector 컬럼 포함)
    """
    # universe lookup — iterrows() 없이 벡터 변환
    uni = universe[["ticker", "name", "sector"]].copy() if "name" in universe.columns else universe[["ticker", "sector"]].copy()
    if "name" not in uni.columns:
        uni["name"] = ""
    universe_map: dict[str, dict] = {
        t: {"name": (n or ""), "sector": (s if s and str(s) != "nan" else None)}
        for t, n, s in zip(uni["ticker"], uni["name"], uni["sector"])
    }

    results: list[dict[str, Any]] = []
    total = len(all_histories)
    passed = 0

    for ticker, hist in all_histories.items():
        if hist.empty:
            continue

        # DataFrame → NumPy 배열 (이후 모든 계산 벡터화)
        close = hist["Close"].to_numpy(dtype=float)
        vol = hist["Volume"].to_numpy(dtype=float)
        high = hist["High"].to_numpy(dtype=float) if "High" in hist.columns else close.copy()
        low = hist["Low"].to_numpy(dtype=float) if "Low" in hist.columns else close.copy()
        # 시가는 거래량 트리거의 봉 방향(양봉/십자형) 판정에만 쓴다.
        # **시가가 없는 소스를 종가로 메우면 조용히 틀린다** — 그러면 몸통이 0이라
        # 모든 대량거래일이 십자형(=매수 신호)으로 잡혀, 고치려던 "방향을 안 본다"는
        # 문제가 그대로 남는다. prices_us._rows_to_df가 KIS 응답에 시가 칸이 없으면
        # 종가로 채우므로(`_first(r, _OPEN_KEYS) or c`) 실제로 생길 수 있는 상황이다.
        # 전 구간 시가=종가면 진짜 봉이 아니라 메운 값이므로 트리거를 끈다.
        open_ = hist["Open"].to_numpy(dtype=float) if "Open" in hist.columns else close.copy()
        has_open = "Open" in hist.columns and not np.array_equal(open_, close)

        # 최소 유동성 (벡터 연산)
        if (close * vol).mean() < MIN_DOLLAR_VOL:
            continue

        # KIS 1년치 이력(lookback_days=380)에서 52주 최고가 직접 계산
        high_52w = float(high.max())
        qualifies, stats = _score_candidate(high, low, close, vol, high_52w)
        if not qualifies or stats["score"] < MIN_SCORE:
            continue

        # matched_bottom 설명 문자열 생성
        drawdown_pct = stats["drawdown"] * 100
        days = stats["days_since_low"]
        vr = stats["vol_ratio"]
        vcp_tag = "VCP ✓" if stats["vcp"] else "VCP ✗"
        ma_tag = "이평 ✓" if stats["ma_align"] else "이평 ✗"

        matched_bottom = (
            f"하락률 {drawdown_pct:.0f}% · 저점 유지 {days}일 · "
            f"거래량 {vr:.2f}배 · {vcp_tag} · {ma_tag}"
        )

        meta = universe_map.get(ticker, {})
        results.append(
            {
                "ticker": ticker,
                "name": meta.get("name") or ticker,
                "sector": meta.get("sector"),
                "similarity": round(stats["score"], 4),
                "matched_standard": "Gold Standard 바닥 특성",
                "matched_standard_ticker": None,
                "matched_bottom": matched_bottom,
                "volume_triggered": (
                    has_open and _is_volume_trigger_today(open_, high, low, close, vol)
                ),
                "close": float(close[-1]),
                # 추천 성적을 나중에 "어떤 특성의 후보가 잘 맞았나"로 쪼개려면
                # 추천 시점의 원본 수치가 남아 있어야 한다. matched_bottom은 사람이
                # 읽는 문자열이라 집계에 못 쓴다 → db.save_recommendation_history가
                # 이 값들을 recommendation_history에 같이 저장한다.
                "drawdown_pct": round(stats["drawdown"] * 100, 2),
                "days_since_low": stats["days_since_low"],
                "vol_ratio": round(stats["vol_ratio"], 4),
                "vcp": stats["vcp"],
                "ma_align": stats["ma_align"],
                "higher_low": stats["higher_low"],
            }
        )
        passed += 1

    results.sort(key=lambda x: x["similarity"], reverse=True)
    top = results[:TOP_N]
    print(
        f"  [pattern_discovery] 완료: {total}개 스캔 → {passed}개 조건 충족 → "
        f"상위 {len(top)}개 저장 (MIN_SCORE={MIN_SCORE:.0%})",
        flush=True,
    )
    return top
